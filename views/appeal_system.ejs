<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= serverName %> — Appeal System</title>
    <style>
        :root{--bg:#050505;--card:#101012;--text:#ffffff;--sub:#a1a1aa;--acc:#ff0055;--acc-glow:rgba(255,0,85,0.5);--border:#27272a;--green:#00ff9d;--blue:#5865F2}
        *{margin:0;padding:0;box-sizing:border-box}
        body{font-family:'Segoe UI',sans-serif;background:var(--bg);color:var(--text);min-height:100vh;display:flex;flex-direction:column;align-items:center}
        
        header{
            width:100%;padding:40px 20px;text-align:center;
            background:linear-gradient(180deg, rgba(255,0,85,0.12) 0%, transparent 100%);
            border-bottom:1px solid var(--border);position:relative;
        }
        
        .back-btn{
            position:absolute;top:20px;left:20px;color:var(--text);text-decoration:none;
            font-weight:600;background:rgba(255,255,255,0.05);border:1px solid var(--border);
            padding:8px 20px;border-radius:30px;font-size:0.85rem;transition:0.3s;
        }
        .back-btn:hover{background:var(--acc);color:#fff;border-color:var(--acc);box-shadow:0 0 15px var(--acc-glow)}
        
        .logout-btn{
            position:absolute;top:20px;right:20px;color:var(--text);text-decoration:none;
            font-weight:600;background:rgba(255,255,255,0.05);border:1px solid var(--border);
            padding:8px 20px;border-radius:30px;font-size:0.85rem;transition:0.3s;
        }
        .logout-btn:hover{background:rgba(255,255,255,0.1);border-color:var(--text)}

        .user-badge{
            display:inline-flex;align-items:center;gap:10px;
            background:rgba(255,255,255,0.03);border:1px solid var(--border);
            padding:6px 16px 6px 6px;border-radius:30px;margin-bottom:15px;
        }
        .user-badge img{width:28px;height:28px;border-radius:50%}
        .user-badge span{font-size:0.85rem;color:var(--sub);font-weight:600}
        
        h1{font-size:2rem;font-weight:800;margin-bottom:5px;text-shadow:0 0 20px var(--acc-glow)}
        .subtitle{color:var(--sub);font-size:0.95rem}
        
        .options-grid{
            display:grid;grid-template-columns:repeat(auto-fit,minmax(300px,1fr));
            gap:30px;max-width:750px;width:90%;margin:60px auto;
            animation:fadeUp 0.6s ease-out;
        }
        @keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
        
        .option-card{
            background:var(--card);border:1px solid var(--border);border-radius:20px;
            padding:45px 30px;text-align:center;text-decoration:none;color:inherit;
            transition:all 0.3s ease;position:relative;overflow:hidden;display:block;
        }
        .option-card::before{
            content:'';position:absolute;top:0;left:0;width:100%;height:4px;
            transition:all 0.3s ease;
        }
        .option-card:hover{transform:translateY(-6px);box-shadow:0 15px 50px -15px rgba(0,0,0,0.5)}
        
        .option-card.submit::before{background:var(--acc);box-shadow:0 0 15px var(--acc-glow)}
        .option-card.submit:hover{border-color:var(--acc)}
        
        .option-card.status::before{background:var(--blue);box-shadow:0 0 15px rgba(88,101,242,0.5)}
        .option-card.status:hover{border-color:var(--blue)}
        
        .card-icon{font-size:3rem;margin-bottom:18px;display:block}
        .card-title{font-size:1.3rem;font-weight:800;margin-bottom:10px}
        .card-desc{color:var(--sub);font-size:0.88rem;line-height:1.5;margin-bottom:22px}
        
        .card-btn{
            display:inline-block;padding:12px 28px;border-radius:30px;
            font-weight:700;font-size:0.82rem;text-transform:uppercase;letter-spacing:1px;
            transition:all 0.3s ease;border:1px solid var(--border);
            color:var(--text);
        }
        .option-card.submit:hover .card-btn{background:var(--acc);border-color:var(--acc);color:#fff;box-shadow:0 0 20px var(--acc-glow)}
        .option-card.status:hover .card-btn{background:var(--blue);border-color:var(--blue);color:#fff;box-shadow:0 0 20px rgba(88,101,242,0.5)}
    </style>
</head>
<body>
    <header>
        <a href="/menu" class="back-btn">← Main Menu</a>
        <a href="/logout" class="logout-btn">Logout</a>
        <div class="user-badge">
            <img src="https://cdn.discordapp.com/avatars/<%= user.id %>/<%= user.avatar %>.png?size=64" alt="">
            <span><%= user.username %></span>
        </div>
        <h1>Appeal System</h1>
        <p class="subtitle"><%= serverName %></p>
    </header>
    
    <div class="options-grid">
        <a href="/appeal/submit" class="option-card submit">
            <span class="card-icon">📝</span>
            <div class="card-title">Submit an Appeal</div>
            <p class="card-desc">If you were permanently banned, you can submit a ban appeal for our staff team to review.</p>
            <span class="card-btn">Start Appeal</span>
        </a>
        
        <a href="/appeal/status" class="option-card status">
            <span class="card-icon">📊</span>
            <div class="card-title">Appeal Status</div>
            <p class="card-desc">Check the current status of your submitted ban appeals and view past decisions.</p>
            <span class="card-btn">View Status</span>
        </a>
    </div>
</body>
</html>
