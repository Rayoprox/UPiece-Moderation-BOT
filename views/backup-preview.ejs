<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Backup Preview - <%= guildName %></title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css">
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
        }

        .header {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
            padding: 30px;
            border-radius: 15px;
            margin-bottom: 30px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .header h1 {
            font-size: 2.5em;
            margin-bottom: 10px;
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .header p {
            font-size: 1.1em;
            opacity: 0.9;
            margin-bottom: 5px;
        }

        .server-icon {
            width: 60px;
            height: 60px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.3);
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2em;
        }

        .content {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
            gap: 20px;
            margin-bottom: 30px;
        }

        .card {
            background: white;
            border-radius: 10px;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
            overflow: hidden;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
        }

        .card-header {
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            color: white;
            padding: 20px;
            display: flex;
            align-items: center;
            gap: 10px;
            font-size: 1.3em;
            font-weight: 600;
        }

        .card-body {
            padding: 20px;
            max-height: 500px;
            overflow-y: auto;
        }

        .item {
            padding: 12px;
            margin-bottom: 8px;
            background: #f5f5f5;
            border-radius: 6px;
            border-left: 4px solid #667eea;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 0.95em;
        }

        .item-name {
            font-weight: 500;
            color: #333;
        }

        .item-badge {
            background: #667eea;
            color: white;
            padding: 3px 8px;
            border-radius: 12px;
            font-size: 0.75em;
            font-weight: 600;
        }

        .channel-category {
            margin-bottom: 15px;
        }

        .category-name {
            font-weight: 600;
            color: #667eea;
            font-size: 0.9em;
            text-transform: uppercase;
            margin-bottom: 8px;
            padding: 8px;
            background: #f0f0f0;
            border-radius: 4px;
        }

        .channel-item {
            padding-left: 25px;
            margin-bottom: 6px;
            position: relative;
        }

        .channel-item::before {
            content: '';
            position: absolute;
            left: 10px;
            top: 50%;
            transform: translateY(-50%);
            width: 8px;
            height: 8px;
            background: #667eea;
            border-radius: 50%;
        }

        .stats {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 15px;
            margin-bottom: 30px;
        }

        .stat-box {
            background: white;
            border-radius: 10px;
            padding: 20px;
            text-align: center;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
        }

        .stat-number {
            font-size: 2.5em;
            font-weight: 700;
            color: #667eea;
            margin-bottom: 5px;
        }

        .stat-label {
            color: #666;
            font-weight: 500;
            font-size: 0.95em;
        }

        .info-bar {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            color: white;
            padding: 15px;
            border-radius: 10px;
            display: flex;
            justify-content: space-between;
            margin-bottom: 30px;
            border: 1px solid rgba(255, 255, 255, 0.2);
            font-size: 0.95em;
        }

        .timestamp {
            opacity: 0.8;
        }

        .text-channel {
            color: #555;
        }

        .voice-channel {
            color: #ff6b6b;
        }

        .stage-channel {
            color: #ffa500;
        }

        .forum-channel {
            color: #a78bfa;
        }

        .permissions-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(150px, 1fr));
            gap: 10px;
            margin-top: 10px;
        }

        .permission-tag {
            background: #e3f2fd;
            color: #1976d2;
            padding: 6px 12px;
            border-radius: 20px;
            font-size: 0.85em;
            font-weight: 500;
            text-align: center;
        }

        .role-color {
            display: inline-block;
            width: 16px;
            height: 16px;
            border-radius: 3px;
            margin-right: 8px;
            vertical-align: middle;
            border: 1px solid rgba(0, 0, 0, 0.1);
        }

        .no-data {
            text-align: center;
            padding: 30px;
            color: #999;
            font-style: italic;
        }

        @media (max-width: 768px) {
            .content {
                grid-template-columns: 1fr;
            }

            .header h1 {
                font-size: 1.8em;
            }

            .info-bar {
                flex-direction: column;
                gap: 10px;
            }
        }

        ::-webkit-scrollbar {
            width: 8px;
        }

        ::-webkit-scrollbar-track {
            background: #f1f1f1;
        }

        ::-webkit-scrollbar-thumb {
            background: #667eea;
            border-radius: 4px;
        }

        ::-webkit-scrollbar-thumb:hover {
            background: #764ba2;
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header">
            <h1>
                <div class="server-icon">
                    <i class="fas fa-server"></i>
                </div>
                <%= guildName %> - Backup Preview
            </h1>
            <p><i class="fas fa-info-circle"></i> Snapshot of your server configuration</p>
            <p class="timestamp"><i class="fas fa-clock"></i> Created: <%= new Date(backupTimestamp).toLocaleString() %></p>
        </div>

        <!-- Statistics -->
        <div class="stats">
            <div class="stat-box">
                <div class="stat-number"><%= channelCount %></div>
                <div class="stat-label">Total Channels</div>
            </div>
            <div class="stat-box">
                <div class="stat-number"><%= roleCount %></div>
                <div class="stat-label">Total Roles</div>
            </div>
            <div class="stat-box">
                <div class="stat-number"><%= categoryCount %></div>
                <div class="stat-label">Categories</div>
            </div>
            <div class="stat-box">
                <div class="stat-number"><%= textChannelCount %></div>
                <div class="stat-label">Text Channels</div>
            </div>
        </div>

        <!-- Info Bar -->
        <div class="info-bar">
            <div>
                <i class="fas fa-lock"></i> Token expires in 24 hours
            </div>
            <div>
                <i class="fas fa-shield-alt"></i> Only server admins can view this page
            </div>
            <div>
                <i class="fas fa-check-circle"></i> Backup is valid and safe to restore
            </div>
        </div>

        <!-- Content Grid -->
        <div class="content">
            <!-- Roles Section -->
            <div class="card">
                <div class="card-header">
                    <i class="fas fa-badge"></i> Roles (<%= roleCount %>)
                </div>
                <div class="card-body">
                    <% if (roles.length === 0) { %>
                        <div class="no-data">
                            <i class="fas fa-inbox"></i> No custom roles
                        </div>
                    <% } else { %>
                        <% roles.forEach(role => { %>
                            <div class="item">
                                <div class="item-name">
                                    <span class="role-color" style="background-color: <%= role.color || '#999999' %>;"></span>
                                    <%= role.name %>
                                </div>
                                <div style="font-size: 0.8em; color: #999;">
                                    <% if (role.hoist) { %>
                                        <span class="item-badge" style="background: #ff9800; margin-right: 3px;">Hoisted</span>
                                    <% } %>
                                    <span class="item-badge">Position: <%= role.position %></span>
                                </div>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

            <!-- Channels Section -->
            <div class="card">
                <div class="card-header">
                    <i class="fas fa-hashtag"></i> Channels (<%= channelCount %>)
                </div>
                <div class="card-body">
                    <% if (channels.length === 0) { %>
                        <div class="no-data">
                            <i class="fas fa-inbox"></i> No channels
                        </div>
                    <% } else { %>
                        <% 
                            const categories = {};
                            channels.forEach(channel => {
                                const catName = channel.parentName || 'No Category';
                                if (!categories[catName]) categories[catName] = [];
                                categories[catName].push(channel);
                            });
                        %>
                        <% Object.entries(categories).forEach(([catName, channelsInCat]) => { %>
                            <div class="channel-category">
                                <div class="category-name">
                                    <% if (catName !== 'No Category') { %>
                                        <i class="fas fa-folder"></i> <%= catName %>
                                    <% } else { %>
                                        <i class="fas fa-layer-group"></i> Uncategorized
                                    <% } %>
                                </div>
                                <% channelsInCat.forEach(channel => { %>
                                    <div class="item channel-item" style="border-left: 0; background: transparent; margin-bottom: 4px;">
                                        <span class="item-name">
                                            <% 
                                                let icon = '📝';
                                                let typeClass = 'text-channel';
                                                if (channel.type === 2) { 
                                                    icon = '🔊';
                                                    typeClass = 'voice-channel';
                                                } else if (channel.type === 5) { 
                                                    icon = '📢';
                                                    typeClass = 'stage-channel';
                                                } else if (channel.type === 15) { 
                                                    icon = '💬';
                                                    typeClass = 'forum-channel';
                                                }
                                            %>
                                            <span class="<%= typeClass %>"><%= icon %> <%= channel.name %></span>
                                        </span>
                                    </div>
                                <% }); %>
                            </div>
                        <% }); %>
                    <% } %>
                </div>
            </div>

            <!-- Permissions Summary -->
            <div class="card">
                <div class="card-header">
                    <i class="fas fa-lock"></i> Permissions Overview
                </div>
                <div class="card-body">
                    <p style="margin-bottom: 15px; color: #666; font-size: 0.9em;">
                        <i class="fas fa-info-circle"></i> Role-specific permissions are preserved in the backup
                    </p>
                    <div style="padding: 10px; background: #f5f5f5; border-radius: 6px; font-size: 0.9em; color: #555;">
                        <p><strong>✓</strong> All role permissions are backed up</p>
                        <p style="margin-top: 8px;"><strong>✓</strong> Channel permission overwrites are preserved</p>
                        <p style="margin-top: 8px;"><strong>✓</strong> Member permissions are not lost</p>
                        <p style="margin-top: 8px;"><strong>✓</strong> Safe to restore anytime</p>
                    </div>
                </div>
            </div>
        </div>

        <!-- Footer -->
        <div style="text-align: center; color: rgba(255, 255, 255, 0.7); margin-top: 30px; padding: 20px;">
            <p>
                <i class="fas fa-lg fa-shield-alt"></i> Your backup is secure and ready to restore
            </p>
            <p style="margin-top: 10px; font-size: 0.9em;">
                Use <code style="background: rgba(0, 0, 0, 0.3); padding: 3px 8px; border-radius: 3px;">/backupload</code> command in Discord to restore this backup
            </p>
        </div>
    </div>
</body>
</html>
