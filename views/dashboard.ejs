<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= bot ? bot.username : 'Bot' %> Panel</title>
    <style>
        :root{--bg:#050505;--card:#101012;--text:#ffffff;--sub:#a1a1aa;--acc:#8b5cf6;--acc-glow:rgba(139,92,246,0.5);--border:#27272a}
        body{font-family:'Segoe UI',sans-serif;background:var(--bg);margin:0;color:var(--text);display:flex;flex-direction:column;align-items:center;min-height:100vh;user-select:none}
        
        header{
            background:linear-gradient(180deg, rgba(139,92,246,0.15) 0%, transparent 100%);
            width:100%;padding:40px 20px;text-align:center;
            border-bottom:1px solid var(--border); margin-bottom:40px; position:relative;
        }
        
        .back-btn{
            position:absolute;top:20px;left:20px;
            color:var(--text);text-decoration:none;font-weight:600;
            background:rgba(255,255,255,0.05);border:1px solid var(--border);
            padding:8px 20px;border-radius:30px;font-size:0.9rem;transition:0.3s;
        }
        .back-btn:hover{background:var(--acc);color:#000;box-shadow:0 0 15px var(--acc-glow);border-color:var(--acc);}

        .profile{display:flex;flex-direction:column;align-items:center;gap:15px}
        .avatar{
            width:120px;height:120px;border-radius:50%;
            border:3px solid var(--border);
            box-shadow: 0 0 30px rgba(0,0,0,0.5);
            transition:transform .3s;
        }
        .avatar:hover{transform:scale(1.05); border-color:var(--acc); box-shadow:0 0 20px var(--acc-glow);}
        
        h1{margin:0;font-size:2.5rem;font-weight:800;text-shadow:0 0 20px rgba(0,0,0,0.5);}
        .sub{font-size:1.1rem;color:var(--sub);margin-top:5px;font-family:monospace}
        
        .nav{
            background:var(--card);padding:10px 15px;border-radius:50px;
            border:1px solid var(--border); margin-top:-30px;
            display:flex;gap:10px;z-index:10; backdrop-filter: blur(10px);
        }
        .link{
            text-decoration:none;color:var(--sub);font-weight:600;
            padding:10px 25px;border-radius:25px;transition:.3s;
        }
        .link:hover{color:var(--text);background:rgba(255,255,255,0.05)}
        .link.active{background:var(--acc);color:#000;box-shadow:0 0 15px var(--acc-glow);}
        
        .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:30px;max-width:1000px;width:90%;margin:50px 0}
        
        .card{
            background:var(--card);padding:40px;border-radius:20px;
            text-align:center; border:1px solid var(--border);
            transition:.3s; position:relative; overflow:hidden;
        }
        .card::before {
            content:''; position:absolute; top:0; left:0; width:100%; height:4px;
            background:var(--acc); box-shadow:0 0 10px var(--acc);
        }
        
        .card:hover{transform:translateY(-5px); border-color:var(--acc); box-shadow:0 10px 40px -10px rgba(0,0,0,0.5);}
        
        .num{font-size:4rem;font-weight:800;color:var(--text);margin:15px 0;line-height:1;text-shadow:0 0 10px rgba(255,255,255,0.1);}
        .label{font-size:1rem;color:var(--acc);font-weight:700;text-transform:uppercase;letter-spacing:2px;}
    </style>
</head>
<body>
    <header>
        <a href="/guilds" class="back-btn">← Switch Server</a>
        <div class="profile">
            <% if (bot?.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%= bot.id %>/<%= bot.avatar %>.png" class="avatar">
            <% } %>
            <div>
                <h1><%= bot ? bot.username : 'System' %></h1>
                <div class="sub">OPERATOR: <%= user.username.toUpperCase() %></div>
            </div>
        </div>
    </header>

    <div class="nav">
        <a href="#" class="link active">Overview</a>
        <a href="/modlogs/<%= guildId %>" class="link">Audit Logs</a>
    </div>

    <main class="grid">
        <div class="card">
            <div class="label">Infractions</div>
            <div class="num"><%= totalModlogs %></div>
            <p style="color:var(--sub);margin:0">Total Database Records</p>
        </div>
        <div class="card">
            <style>.card:nth-child(2)::before{background:#00ff9d;box-shadow:0 0 10px #00ff9d}</style>
            <div class="label" style="color:#00ff9d">Active Tickets</div>
            <div class="num"><%= activeTickets %></div>
            <p style="color:var(--sub);margin:0">Open Support Channels</p>
        </div>
    </main>
</body>
</html>