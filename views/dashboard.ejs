<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= bot ? bot.username : 'Bot' %> Panel</title>
    <style>
        :root{--bg:#f2f3f5;--card:#fff;--text:#23272a;--sub:#99aab5;--acc:#5865F2;--acc-h:#4752c4}
        body{font-family:'Segoe UI',system-ui,sans-serif;background:var(--bg);margin:0;color:var(--text);display:flex;flex-direction:column;align-items:center;min-height:100vh;user-select:none}
        header{background:linear-gradient(135deg,var(--acc),var(--acc-h));width:100%;padding:40px 20px;text-align:center;color:#fff;box-shadow:0 4px 15px rgba(88,101,242,.3);margin-bottom:40px}
        .profile{display:flex;flex-direction:column;align-items:center;gap:15px}
        .avatar{width:120px;height:120px;border-radius:50%;border:5px solid rgba(255,255,255,.3);transition:transform .3s}
        .avatar:hover{transform:scale(1.05)}
        h1{margin:0;font-size:2.5rem;font-weight:700}
        .sub{font-size:1.1rem;opacity:.9;margin-top:5px}
        .nav{background:var(--card);padding:10px 30px;border-radius:50px;box-shadow:0 4px 6px rgba(0,0,0,.05);margin-top:-25px;display:flex;gap:20px;z-index:10}
        .link{text-decoration:none;color:var(--text);font-weight:600;padding:10px 20px;border-radius:25px;transition:.2s}
        .link:hover{background:#f0f0f0;color:var(--acc)}
        .link.active{background:var(--acc);color:#fff}
        .link.out:hover{background:#ed4245;color:#fff}
        .grid{display:grid;grid-template-columns:repeat(auto-fit,minmax(280px,1fr));gap:30px;max-width:1000px;width:90%;margin:40px 0}
        .card{background:var(--card);padding:30px;border-radius:16px;text-align:center;box-shadow:0 4px 12px rgba(0,0,0,.05);transition:.2s;border-top:5px solid var(--acc)}
        .card:hover{transform:translateY(-5px);box-shadow:0 8px 24px rgba(0,0,0,.1)}
        .num{font-size:3.5rem;font-weight:800;color:var(--acc);margin:10px 0;line-height:1}
        .label{font-size:1.2rem;color:var(--sub);font-weight:600;text-transform:uppercase;letter-spacing:1px}
    </style>
    <script>
        document.addEventListener('contextmenu', e => e.preventDefault());
        document.addEventListener('keydown', e => {
            if (e.key === 'F12' || (e.ctrlKey && e.shiftKey && ['I','J','C'].includes(e.key.toUpperCase())) || (e.ctrlKey && e.key === 'u')) {
                e.preventDefault();
            }
        });
    </script>
</head>
<body>
    <header>
        <div class="profile">
            <% if (bot?.avatar) { %>
                <img src="https://cdn.discordapp.com/avatars/<%= bot.id %>/<%= bot.avatar %>.png" class="avatar">
            <% } %>
            <div>
                <h1><%= bot ? bot.username : 'Bot' %></h1>
                <div class="sub">Management Panel • <%= user.username %></div>
            </div>
        </div>
    </header>

    <div class="nav">
        <a href="/" class="link active">Dashboard</a>
        <a href="/guilds" class="link">Servers</a>
        <a href="/modlogs" class="link">Logs</a>
        <a href="/logout" class="link out">Logout</a>
    </div>

    <main class="grid">
        <div class="card">
            <div class="label">Total Infractions</div>
            <div class="num"><%= totalModlogs %></div>
            <p style="color:var(--sub);margin:0">Recorded Actions</p>
        </div>
        <div class="card" style="border-top-color:#3ba55c">
            <div class="label">Active Tickets</div>
            <div class="num" style="color:#3ba55c"><%= activeTickets %></div>
            <p style="color:var(--sub);margin:0">Open Support Channels</p>
        </div>
    </main>
</body>
</html>