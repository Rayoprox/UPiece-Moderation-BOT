<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Access Denied</title>
    <style>
        body {
            font-family: 'Segoe UI', 'Helvetica Neue', Helvetica, Arial, sans-serif;
            background-color: #050505;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            color: #fff;
        }
        .card {
            background: #101012;
            padding: 60px;
            border-radius: 20px;
            box-shadow: 0 0 50px rgba(255,0,85,0.1);
            text-align: center;
            max-width: 500px;
            width: 90%;
            border: 1px solid #27272a;
            border-top: 4px solid #ff0055;
            animation: shake 0.5s ease-out;
        }
        .icon { font-size: 4rem; margin-bottom: 20px; display: block; filter: drop-shadow(0 0 10px #ff0055); }
        h1 { color: #ff0055; margin: 0 0 15px 0; font-size: 2.5rem; font-weight: 800; letter-spacing: -1px; }
        p { font-size: 1.1rem; line-height: 1.6; color: #a1a1aa; margin-bottom: 40px; }
        b { color: #fff; }
        
        .btn {
            display: inline-block; padding: 15px 40px;
            background-color: #ff0055; color: #000;
            text-decoration: none; border-radius: 50px;
            font-weight: 800; text-transform: uppercase; letter-spacing: 1px;
            transition: 0.2s; box-shadow: 0 0 20px rgba(255,0,85,0.4);
        }
        .btn:hover { transform: scale(1.05); background-color: #ff3377; }
        
        @keyframes shake {
            0% { transform: translateX(0); } 25% { transform: translateX(-5px); } 50% { transform: translateX(5px); } 75% { transform: translateX(-5px); } 100% { transform: translateX(0); }
        }
    </style>
</head>
<body>
    <div class="card">
        <span class="icon">⛔</span>
        <h1>ACCESS DENIED</h1>
        <p><%- message %></p>
        <a href="/" class="btn">TERMINATE SESSION</a>
    </div>
</body>
</html>
